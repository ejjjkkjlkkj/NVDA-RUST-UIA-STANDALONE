# Reference material

Official screen readers and platform documentation are kept as read-only comparison references. NVDA is pinned by commit in this directory so behavior and architecture comparisons are reproducible. Reference source must never be vendored into the Rust runtime as an implementation shortcut.
