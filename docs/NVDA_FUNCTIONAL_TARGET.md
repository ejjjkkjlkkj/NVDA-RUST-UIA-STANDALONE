# Functional target

The project must behave like a real screen reader for a blind keyboard user, not like a UIA event monitor.

A milestone is acceptable only when it provides a user-facing operation (navigation, reading, editing, web interaction, speech control, braille, configuration) and is validated in a real application with keyboard-only interaction.

NVDA is the reference for capability coverage and user expectations, not the implementation base.
